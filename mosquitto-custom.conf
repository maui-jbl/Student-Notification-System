# Listen on ALL network interfaces (required for external connections)
listener 1885 0.0.0.0
protocol mqtt

# WebSocket listener (for mobile/web apps)
listener 9001 0.0.0.0
protocol websockets

allow_anonymous true